before_script:
  - docker -D info

test:
  script:
    - docker pull cypress/included:3.2.0
    - docker run -v $PWD:/e2e -w /e2e cypress/included:3.2.0

after_script:
    - sudo chown gitlab-runner:gitlab-runner -R cypress
    - sudo rm -r cypress/screenshots/
    - sudo rm -r cypress/videos/