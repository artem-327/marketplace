before_script:
  - docker -D info
  - df -h
  - cd cypress/
  - set e+x
  - docker build -t cypress/artio .

test:
  script:
    - cd ..
    - set -x
    - docker run -v $PWD:/test -w /test -u appuser cypress/artio