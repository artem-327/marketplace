before_script:
  - docker -D info
  - docker system prune -f
  - df -h
  - df -i
  - docker system df
  - cd cypress/
  - set e+x
  - docker build -t cypress/artio .

test:
  script:
    - cd ..
    - docker run -v $PWD:/e2e -w /e2e cypress/included:3.2.0