import styled from 'styled-components'
import { ImageSearch } from '@material-ui/icons'

export const DivLogoWrapper = styled.div`
  padding: 20px;
`

export const ImageSearchStyled = styled(ImageSearch)`
  font-size: 64px !important;
`
